var rest = require('rest-sugar');

function init(app, prefix, apis, queries, auth) {
    // TODO
}
exports.init = init;

// TODO: hook up models and specific logic. probably need to construct model
// schemas dynamically somehow (to queries api?)
